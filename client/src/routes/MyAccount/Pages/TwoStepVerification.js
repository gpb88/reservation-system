import * as React from 'react';
import { Container, Grid, Button, Typography, Checkbox } from '@mui/material';
import 'styles/my-account.css';

export default function TwoStepVerification(props) {
    React.useEffect(() => {}, []);

    return <Container className='change-password' disableGutters></Container>;
}
